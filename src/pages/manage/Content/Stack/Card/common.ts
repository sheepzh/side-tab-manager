import { CSSProperties } from "react"

export const GROUP_HEADER_STYLE: CSSProperties = {
    padding: 0,
}

export const GROUP_HEADER_INNER_STYLE: CSSProperties = {
    paddingInlineStart: 7,
    paddingInlineEnd: 5,
    height: 40,
    cursor: 'pointer',
}

export const GROUP_BODY_STYLE: CSSProperties = {
    padding: '8px 2px',
}